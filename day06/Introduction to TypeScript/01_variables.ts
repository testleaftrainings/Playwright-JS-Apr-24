let companyName: string = "Qeagle";
console.log(companyName);
/**
 * Data type in TS - any, never, Array (homogneous), Tuple (hetero)
 * 
 * 
 * 
 */

let firstname = true;       //Implicit type inference
console.log(firstname);

let browserName:string = "Edge";    //Explicit type inference
console.log(browserName);

/**
 * Type Inference - infering the data type
 *      1. Implicit type inference
 *          When TS is able to infer the data type on its own from the value that 
 *             you are assignment
 * `    2. Explicit type inference
 *          When the user explicitly mention the data type 
 * 
 * 
 */
